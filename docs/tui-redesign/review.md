# Review — TuiRedesign

One line per group, written by the cycle that built it, after auditing its own diff. This file is the codebase's memory of what it gets wrong: read it before building.

- reviewed s1-cell-rendertablefit a0bcb37, s1-primitives 8c9f314, s1-columns 50e891f: REPAIRED — the exact-string tests each pinned one width, leaving the group's actual guarantee ("a rendered line never exceeds its budget") asserted only at the widths that happened to be written down; added `TestRenderTableFitNeverExceedsTheWidth` and `TestColumnsNeverExceedsTheWidth`, which sweep every width against wide runes, colour and each weight/gap shape, and confirmed both catch a dropped budget clamp, a forgotten separator, a forgotten rule cell and a forgotten ellipsis cell. Handoff carried two claims the diff did not support: a test count that was already stale and `ui/ec2_panel.go:266` for a symbol that sits at 267; both corrected. Behaviour audited by mutation rather than by reading: 19 mutants across all three members (colour-before-truncate, remainder to the first flexible column, decolorise-then-cut, no reset after a cut, padding on raw bytes, no stacking below the threshold, dropped NaN and zero-time guards, badge without its icon, header collapsing to two lines) were each introduced and all 19 failed a test. No stubs: every member's ticketed surface is implemented.
- reviewed s2-inpanel-empty 21c5126, s2-left-profiles-ecs a1c3d7f, s2-left-ec2-s3 d30c315: REPAIRED (d4583e8) — the group shipped rows whose identifying column could be rendered away. `fitColumnWidths` pays content-sized columns their full width first and leaves the flexible column the remainder, so at any panel narrower than ~48 cells the ECS cluster row rendered as `▶  9 services 12 running / 1 pending ● …` with no cluster name at all, and the EC2 row had the same shape (caught during the build by the ticket's own mandated 60-char-name-at-width-40 case, and patched there with weights, which only worked because EC2's content columns happen to be narrow). Measured every alternative weighting across widths 30-100 first: all of them fix the collapse by truncating `12 running / 1 pending` at widths 80 and 100 where 15 cells sit idle in the name column's padding, because a flexible column's proportional share ignores what its content needs. Fixed at the cause instead: a flexible column claims `flexibleFloor` (8) before the content columns are paid, and the pre-existing left-to-right clamp reclaims those cells from the trailing columns. Nothing changes at any width where the row already fitted, and s1's `TestRenderTableFitNeverExceedsTheWidth` sweep stayed green throughout. Behaviour audited by mutation, not by reading: 30 mutants across the three members and the repair (empty branch removed, message unmuted, blank-message guard dropped, message shown alongside rows, healthy claimed while tasks pending, draining cluster called deploying, badge bullet dropped, empty status left bare, weights off by one, profile bold dropped, status colour dropped, zero attribute emitting a reset pair, migrated panel filtering on nothing, fit path falling back to RenderTable, view width ignored, name unbolded, id unmuted, unnamed instance blank, name column back to zero width, columns dropped, floor removed, floor uncapped, floor counted once rather than per column, floor swallowing the slack) were each introduced and all failed a test — two only after the test that should have caught them was strengthened, which is what the surviving mutants were for. One live bug was found by a test rather than by review: `ColoredString(text, 0)` wraps text in a reset pair, so the string form of the services table emitted `\x1b[0mweb\x1b[0m`; the zero-value rule now lives once, in `Cell.Rendered`. Handoff claims checked against the diff and all held. No stubs: every member's ticketed surface is implemented.
- reviewed s2-left-eks-ecr 03f6ea4, s2-left-secrets 401c10f, s2-left-vpc 8ce1d70: REPAIRED — one defect was found by measurement during the build and two more by the audit. (1) The VPC row shipped with the id on a proportional weight, which under-pays a column whose content is a fixed 21 cells: at width 60 the id rendered `vpc-0123456789…` while the label column sat on 28 cells of padding. Sized the id to its content instead, so it shows whole from ~50 up and gives way only where the panel is genuinely tight; `TestVPCRowShowsTheWholeIDOnceThePanelHasRoom` pins it. This is the same class as the previous group's repair (a flexible share ignores its content), seen from the other end — there it starved a column at narrow widths, here it starved one at wide. (2) The EKS narrow-width test asserted the date only by `Contains`, which an untrimmed stamp also satisfies, so the whole point of `createdDate` survived that test; it now asserts the row ENDS on a whole date. (3) The migration deleted the EKS panel's `if status == "" { status = "-" }` branch and nothing covered what replaced it; added the empty-status case, which is the style table's `?` icon alone. Handoff claims checked against the diff: two did not hold and are corrected — `ui/eks_panel.go:144` for a symbol my own edit had moved to 143, and "under 150 lines across four files" for what is 172 production insertions and 48 deletions across ten. Behaviour audited by mutation, not by reading: 12 mutants (EKS badge back to icon-only, `createdDate` returning the whole stamp, EKS weights one short, ECR mutability matched by equality so an exclusion variant falls through, the exclusion star never applied, the `●` dropped, the scan column unlabelled, `rotationDays` without its `AutomaticallyAfterDays` nil guard, the rotation-on fallback printing `rotation 0d`, pending-deletion uncoloured, the vpc id unmuted, VPC weights back to proportional) were each introduced and all 12 failed a test — including `TestBadge`, whose failure under a gutted `BadgeCell` is what shows the `Badge` refactor preserved its behaviour. No stubs: every member's ticketed surface is implemented. Not covered: no headless per-panel render test for ECR, secrets or VPC (the EKS one exists because the fit path broke it); the wiring is one line per panel and was verified by reading the diff.
- reviewed s3-selection-identity 88b0aac, s3-selection-theme 88b0aac: REPAIRED (3c8a87b) — the group's tests asserted the METHOD and never the WIRING. Both reload tests called `SetItemsKeepSelection` with their own inline key closure, so mutating `loadEC2List` to key on the `Name` tag instead of the instance id left the entire suite green: a panel keyed on a duplicated or absent field would have shipped invisibly, which is the one mistake this ticket exists to prevent (EC2 Name tags are absent on plenty of instances and identical across an autoscaling group). Each panel's key is now a named function the loader and the test share, plus `TestSelectionKeysAreTheResourceIdentity` pins all eight against fixtures where the identifying field and the displayed one differ. Behaviour audited by mutation, not by reading: 16 mutants (re-find loop deleted, empty-key guard dropped, key captured after the swap instead of before, match resets to the top, comparison inverted, profile pin ungated, profile pin removed, and one wrong key per panel — EC2 on the name, VPC on the CIDR, ECS on the display name, EKS on the version, secrets/S3/ECR/profile on nothing) were each introduced and 15 failed a test; two mutants that did not compile were rewritten so they were actually tested rather than counted. The one survivor is equivalent, not a gap: dropping the early `return` makes the LAST matching row win instead of the first, and no panel's key repeats within a list, so nothing can observe it — pinning it would assert behaviour nothing depends on. Handoff claims checked against the diff: `ui/secrets_panel.go:52` and the eight wired loaders held; the `verified:` line did not (it implied the EC2 test covered the wiring) and is corrected in place. `s3-selection-theme` shipped no code on purpose and the audit re-checked the evidence rather than the absence: `SelFgColor = ColorRed` on a highlighted headless view draws `fg=default bg=navy bold=true`, so the ticket's config key and Settings row would have been inert; it is closed VOID in the backlog with the revival condition, and accounted for in `built.md` because the selector and the DoD both read that file by id. No stubs: the one member that was built implements its ticketed surface.
- reviewed s4-overview-machinery 74fbfe3: REPAIRED (dfa37ce) — the audit found a live crash the group made more likely, and two mutants that showed a stated guarantee was asserted nowhere. (1) `ContextState.mainTabIdx` is held across a tab set that can shrink: `resetDependentPanelState` resets the ECS drill level on a profile switch without touching the index, unlike `drillECS` which does, so a user on the services level's last tab crashed the moment the cluster list reloaded — reproduced as `index out of range [5] with length 4` before the fix. Pre-existing (the gap was indices 3-4 before this ticket), and prepending Overview widened it by one, so it is repaired at the invariant: `GetCurrentMainTab` clamps to the first tab and the clamp STICKS, because `renderContext` reads the raw index into main's `TabIndex`. Fixing the one forgetful call site was rejected — that is the mistake the call site already made once. (2) `Wrap:false` and the `mainBelongsToQ` guard both survived mutation: nothing asserted that an overview turns main's wrapping off, and nothing asserted that a resize leaves the chat screen holding main. Both have tests now. (3) `README.md:153`'s config sample listed two of the three refresh keys; `overviewSeconds` added with its 0-turns-it-off note. Behaviour audited by mutation, not by reading: 22 mutants (the non-positive interval guard removed, 0 falling through to a ticker, the generation snapshotted after the render, the generation guard removed, the width watcher firing on every layout pass, the width never recorded, ObjectKey not cleared, the chat guard removed, the width capture replaced by a constant, wrap on for each task path, the Overview tab taking another tab's key, EC2 naming the wrong resource, VPC and ECS services losing the tab, the unavailable line unmuted, the default interval changed, the yaml key renamed, the clamp removed, off by one, and not sticking) were each introduced and all 22 failed a test — five of them only after the test that should have caught them was written or strengthened, which is what the survivors were for. One mutant is equivalent and its guard was REMOVED rather than kept: resolving the tab before reading `TabIndex` in `renderContext` looked like it prevented a stale header, but `GetCurrentContextKey` runs first on every path and clamps already, so the reorder plus its comment was a warning about something that cannot happen. Handoff claims checked against the diff: `ui/layout.go:63` and `ui/gui.go:176` were both off by my own edits and are corrected to 60 and 179; the `verified:` line understated its own coverage after the repairs and is corrected in place; the seven registry citations and the UI-loop claim held (every `HandleSelect` caller — keybindings, `focus.go:56`, `main_panel.go:81`, `afterRerender`, `secrets_actions.go:124`, and `command_bar.go:117` through the registry — is on the gocui loop). No stubs in the ticketed surface: the plumbing, width capture, generation guard, interval and wiring are all implemented; the tab CONTENT is deliberately a stated `unavailable` line that stages 5-7 replace, which is scope, not a stub.
- reviewed s4-secrets-overview 7226b1a: CLEAN — the audit found nothing to repair, and the one defect this group did produce was caught during the build rather than here: `Columns` stacks its blocks below `minTwoColWidth` and handed each back at its own width, so with wrap off an ARN ran past the pane edge with no ellipsis to mark it. That is s1 code and I changed it anyway (265d1a0, its own commit) because this ticket is its first consumer with content wider than a terminal and every stage 5-7 overview inherits the fix; `TestColumnsNeverExceedsTheWidth` used to skip every width below the threshold as "the documented fallback rather than a budget" and now sweeps it, with only passthrough exempt. Behaviour audited by mutation, not by reading: 18 mutants (stacked truncation dropped on each of its two paths, nil stages rendered as `[]`, AWSPENDING winning the colour over AWSCURRENT on a joint version, the 15-row cap removed, the hidden-version count silenced, `orNone` and `secretTime` returning blank instead of `none`, a timestamp losing its relative half, pending deletion never stated, `Not replicated` blanked, the resource-policy branch inverted so an absent policy claims to be configured, the no-tags line blanked, kv labels unpadded, the cadence badge falling through to "Rotation on", the `insync` alias removed so a healthy replica draws `?`, a failed overview dropping its reason, and the secret's own value spliced into the pane) were each introduced and all 18 failed a test. One mutant was written to SURVIVE and does, which is the honest shape of this group's coverage: replacing the fetched `SecretDetails` with one carrying only the name leaves the whole suite green, because `aws.Client.Secrets` is a concrete `*secretsmanager.Client` with no interface seam and `NewGui` is only ever handed a nil client, so nothing in the ui package can serve a `GetSecretDetails` response. That gap is repo-wide and pre-existing — every panel's tab render has the same shape — so it was left rather than repaired here; it is one interface on `aws.Client`, not seven tests, and stages 5-7 all inherit it. Handoff claims checked against the diff: the four cited anchors (`presentation/secrets.go:52`, `secrets_panel.go:118`, `columns.go:52`, `primitives.go:115`) all resolve, and the `verified:` line's two admissions (no wiring coverage, no connected check) are both accurate — the staging profile's SSO token has expired and re-authenticating is interactive. The cap of 15 is published in the backlog line and the constant and they agree. No stubs: every state the ticket enumerates is implemented and asserted.
- reviewed s5-cw-getmetricdata 649fb8b, s5-ec2-datalayer 6fd562c: REPAIRED (3b3d9db) — the group shipped green with four assertions that could not fail. Behaviour audited by mutation, not by reading: 17 mutants across both members (oldest datapoint instead of newest, first instead of newest, an empty series reported present, results collapsed to one key, network in reading the network out result, a Sum asked as an Average, the period changed, the window shrunk below the publish interval, the instance dimension dropped, a metric dropped from the batch, both metric guards, volumes matched by position, an unanswered device overwritten, volumeIDs keeping device-less entries, DescribeVolumes calling out for an empty list, the cache never read, never written, the empty-type guard, throughput and encryption ignored, an absent reading falling through to its formatter, and a whole metric row dropped) were each introduced; 16 failed a test and the 17th is equivalent — refusing to overwrite an existing cache entry changes nothing, because a type's specs are static and the only writer runs on a miss. Five of the 16 failed only after the test that should have caught them was fixed, which is what the survivors were for: (1) the period assertion compared the request against the `metricPeriod` constant, so it agreed with whatever the constant was changed to and pinned nothing; it pins the literal 300 now, and `metricWindow` — previously asserted nowhere — is checked against the number of publish periods it has to span. (2) Both empty-argument guards were masked by their neighbour: with a nil client an unguarded empty id or type still errors, on the client, so "it errored" could not tell which guard fired; each now asserts the guard it means, and the metrics one is exercised against a non-nil client so only the id guard can answer. (3) `applyVolumes` documents that a device missing from the response is left as it was, but the test's devices were zero-valued, so "left alone" and "overwritten with zeroes" were the same picture; the devices carry real values now. (4) `GetInstanceTypeInfo` mapped a response and cached it as two adjacent statements and deleting the cache write was invisible to every test, so map-and-store is one named step (`rememberInstanceType`) a test can drive without an SDK seam. The harness itself was rebuilt mid-audit after three "kills" turned out to be compile failures (an unused `ok`, an unused `byID`, an unused `cached`), which prove the compiler works and nothing else; it now reports a non-building mutant separately and every mutant is written to compile. Handoff claims checked against the diff: all eleven cited anchors resolve at HEAD, and the two claims the repair invalidated (the `verified:` coverage line and the two-statement cache write) are corrected in place. Not covered, and stated rather than papered over: neither `GetInstanceMetrics` nor `GetInstanceDetails` is reachable from a test — `aws.Client`'s SDK clients are concrete structs with no interface seam, so removing the `applyVolumes` call from `GetInstanceDetails` entirely leaves the suite green. That is the repo-wide gap s4-secrets-overview recorded, one interface rather than a per-ticket fix, and it is unchanged here. No stubs: every state both tickets enumerate is implemented and asserted.
- reviewed s5-ec2-overview 85f4366: REPAIRED (e6e9611) — the group shipped green with two assertions that could not fail, and measuring one of them found a limit the handoff had implied did not exist. Behaviour audited by mutation, not by reading: 34 mutants across the data layer, the formatter and the wiring (wg.Wait removed, the errs map written unlocked under -race, the details fetch swapped back to the one that pulls DescribeAddresses onto the ticker, a whole section never fetched, three nil-client guards and an empty-id guard, the address tail dropped from GetInstanceDetails, the throughput column forced on and forced off, the unencrypted amber removed and the encrypted case amber-ed, an empty device list rendering a table, the unavailable reason dropped, a failed address lookup reading as an absence, a nil ASG membership reported as a failed lookup, an absent metric rendered as a value, an unparseable launch stamp blanked, an unnamed instance left blank, the section join, the stacked join, ColumnWidth off by one and with the gap uncharged, overviewGap disagreeing with the rule it sizes, the volume id put back into the table, the memo never read and keyed on each half alone, the errors never copied onto the overview, the console stamp dropped and its zero guard removed) were each introduced; 32 failed a test, one is equivalent, and five that did not COMPILE on the first pass were rewritten until they did rather than counted as kills — an unused `mu`, an unused `column` and an unused `slices` import prove the compiler works and nothing else. Three of the 32 failed only after the test that should have caught them was written or strengthened, which is what the survivors were for: (1) `TestInstanceOverviewStorageKeepsEncryptionAtTheTwoColumnThreshold` asserted that the pane CONTAINS "unencrypted", which a cut row satisfies just as well, so the volume-id column could be put back and the flag truncated with the suite green; it asserts the boundary now, that every storage row ENDS on a whole flag. (2) Nothing asserted that sections inside a column are separated by a blank line, so joining them with a single newline left Metrics and Storage reading as one list. (3) Nothing distinguished the two halves of the extras memo key on its own. The one equivalent mutant is `instanceStorageBlock` being built for the pane width instead of its column: measured on both a normal row and the widest row EBS can produce, RenderTableFit's left-to-right clamp and `truncateStyled`'s blind cut land on the same characters and carry the same colour, so nothing can observe the difference today — `ColumnWidth` is kept because it is correct by construction, not because a test proves it, and the mutants that CAN be observed (the gap uncharged, `overviewGap` disagreeing with its rule) both die. One real limit was found by measurement rather than by reading and is pinned rather than repaired: an io2 Block Express volume (16384 GiB, 64000 IOPS, 4000 MiB/s) overflows a 52-cell column and the encryption flag is cut to `encr…`/`unen…`; the cut lands after the letters that separate the two states and the amber survives it, so the row still reports the only thing on it anybody acts on. Handoff claims checked against the diff: all ten cited anchors resolve at HEAD, and the one claim the review invalidated — that dropping the volume id buys the flag room, stated without a limit — is corrected in place. A spec question the audit raised and closed with evidence rather than a change: the overview refetches metrics on its 2s interval where spec.md:14 wants ~60s, which is `s8-refresh-engine`'s ticket in as many words ("overview tickers refetch metrics only when MetricsSeconds elapsed"), so it is recorded as a thread rather than built here. No stubs: every state the ticket enumerates is implemented and asserted.
- reviewed s6-ecs-clusterdata 70e59f5 b8d57d7, s6-ecs-image ff71d1d: REPAIRED (83cd7a7) — the group shipped green with the ticket's own headline change asserted nowhere, and a duplicated call that only the audit's own mutant exposed. Behaviour audited by mutation, not by reading: 24 mutants across both members (ShortImageRef dropping the port rule so a `localhost:5000/` host survives, the essential scan skipped and never run, newestECSTask accepting STOPPED tasks and ignoring StartedAt, the sidecar count off by one, the desired image not marked desired, any deployment's revision accepted instead of the PRIMARY one, the singular sidecar wording lost, an unresolved image rendering blank instead of `unavailable`, a desired image labelled running, the image label collapsed to one heading, the Insights rows shown with no data behind them, the taskdef cache holding floating references, service CPU put back on the ECS/ContainerInsights namespace, the Insights extras requested unconditionally, the STATISTICS+SETTINGS Include dropped, the setting mapped as empty and never recorded, statistics dropped off the cluster, the essential flag dropped on a running container and on a task definition container, the deployment's revision not mapped, and every container paired with the first definition instead of its own) were each introduced; all 24 failed a test after the repairs, and four mutants that did not COMPILE were rewritten until they did rather than counted as kills — an unused `cd` proves the compiler works and nothing else. Five mutants survived the first pass and every one of them was a one-line field mapping sitting inside a method that needs a live SDK client, which is the repo-wide seam gap the last three reviews recorded, met here at five sites at once: `Include: [STATISTICS, SETTINGS]` (the ticket's headline change, and both fields come back silently absent rather than as an error), the Insights setting being recorded, `Essential` on a running container and on a task definition container, and the deployment's task definition. The first four decide whether the service claims to be running the right container's image, so they are repaired rather than recorded: each mapper is now a function a test reaches, `clusterDescribeFields` names the Include list, and `buildECSTask` — which takes its task definition loader as a parameter and therefore never needed a seam — is tested through its real entry point with the sidecar listed FIRST, so a mapper keyed on position rather than on name dies. The recording is repaired by deletion instead of by a test: `ingestECSClusters` maps a described page and records the setting as one step, because the setting has no other reader and mapping without recording would have cost every later metrics fetch its extras silently. That refactor is also what surfaced the real defect — `recordClusterInsights` was being called TWICE, once per page inside the ingest and once more over the whole list in `ListECSClusters`, which is why the mutant that deleted one of them survived; the outer call is gone. Two container-building branches in `buildECSTask` collapsed into one mapper taking a nil definition, which is what the else branch was open-coding. Handoff claims checked against the diff: the anchors resolve, with one correction — the handoff cites the recording as happening in the cluster list, and after the repair it happens in `ingestECSClusters`, which the cluster list calls per page. Numbers published twice and checked: `ecsMetricPeriod` is 60 and the service panel captions its readings "1-min avg"; the query test pins the literal 60, so the caption cannot quietly stop being true. Not covered, and stated rather than papered over: no live AWS call was made — the staging profile's SSO token has expired ("Token has expired and refresh failed") and re-authenticating is interactive, so AWS/ECS returning data for the ClusterName+ServiceName pair is carried from the ticket, not re-measured; and `ListECSClusters`, `ListECSServices` and `DescribeTaskDefinitionDetail` themselves remain unreachable from a test for the same concrete-SDK-client reason, so the one line that calls each extracted mapper is verified by reading. No stubs: both tickets' enumerated states are implemented and asserted, including multi-container, the sidecar-only difference, and zero running tasks.
- reviewed s6-ecs-cluster-overview df2b88f 5dc0f7b: REPAIRED (8e64e0c) — the group shipped green with its own headline data path unasserted and four assertions that could not fail. Behaviour audited by mutation, not by reading: 45 mutants across the data layer, the formatter and the wiring (the CONFIGURATIONS ask dropped, the inner execute-command nil guard removed, the region and the execute-command setting each not carried onto the cluster, the rollout state / reason / failed-task count each dropped from the deployment, the essential container ignored, the sidecar count off by one, ListTasks always sending ServiceName and dropping its page token, a ServiceName dimension added to the cluster query, the cluster queries moved to AWS/ECS, a metric dropped from the batch, results mapped by position, both `UtilizationPercent` absence rules, the cluster-name guard, the metrics call made unconditionally, `InsightsOff` never recorded, `wait` removed from the fan-out, an empty rollout state read as unsettled, a rollout still in progress outranking lost tasks, a failed rollout rendered amber, the scaling case dropped, the launch-type fallback removed, the dedupe and the sort each dropped, a failed services fetch assumed to be Fargate, the empty-services and error fallbacks in the header, a gauge drawn from an absent reading, the Insights-off branch removed, the task cap removed and its hidden count silenced, both tables left in response order, an unreadable image rendered blank, the service name content-sized, both overview headers left uncut, empty Insights read as disabled, an unconfigured execute-command blanked, and a completed rollout's reason given a line) were each introduced; 44 failed a test and one is equivalent. Three mutants that did not COMPILE on the first pass were rewritten until they did rather than counted as kills — an unused `rolloutFailed` and a dangling width argument prove the compiler works and nothing else. Five of the 44 failed only after the test that should have caught them was written or strengthened, which is what the survivors were for: (1) `listTasksInput` did not exist — the empty-`ServiceName` decision, which is the whole of whether the cluster-wide task listing works against real AWS, sat as a literal at a call site behind a concrete SDK client, so nothing could reach it; it is a named function now with the page token asserted alongside it, the same repair s6-ecs-clusterdata made for its own mappers. (2) and (3) both tables sort on a copy so rows cannot reshuffle under the cursor between two-second refreshes, and both fixtures were already in sorted order, so deleting either sort was invisible; they render out of order now. (4) the launch-type line was asserted with `Contains`, which `EC2, FARGATE, FARGATE` satisfies exactly as well as `EC2, FARGATE`, so the deduplication survived its own test; it is asserted as the whole line now, through a `lineContaining` helper, which is the same shape as the EKS date defect this file already records. The one equivalent mutant is the tasks table's image weight, and it was measured rather than argued: rendering `{0,0,1}` against `{0,0,0}` across every width from 20 to 200 on both a short and a long image shape produces byte-identical output at all 362 combinations, because the image is the last column and a flexible share and a content size land on the same cells. The weight is kept because it stays correct if a column is ever added after it, not because a test proves it, and the comment claiming it beat a task id for space was corrected to say that. One live defect was found by the width sweep rather than by reading, and it was NOT this group's alone: an overview header spans the pane rather than a column, so `Columns` never measures it, and with wrap off it ran off the edge unmarked — at width 40 the cluster header was 56 cells. `FormatInstanceOverview` has the identical shape and is reachable at 60 cells with a 31-character Name tag, so both are cut with `truncateBlock` and the EC2 width sweep now carries a long-named instance, which is what fails without the cut; that is another group's formatter and it was repaired here because it is the same one-line call and its own suite is what shows nothing else moved. Handoff claims checked against the diff: all nineteen cited anchors resolve at HEAD, and the two the repair invalidated (the ListTasks guard's location and the commit count in `verified:`) are corrected in place. Numbers published twice and checked: `ecsMetricPeriod` is 60 and both the service and the cluster query tests pin the literal, so the panel's "1-min avg" caption cannot quietly stop being true. Not covered, and stated rather than papered over: no live AWS call was made (the staging profile's SSO token is expired and re-authenticating is interactive), so `ECS/ContainerInsights` answering for a ClusterName-only dimension is carried from the shipped Config tab that already queries it, not re-measured; and `ListECSClusters`, `ListECSServices` and `ListECSTasks` themselves remain unreachable from a test for the concrete-SDK-client reason, so the one line calling each extracted mapper is verified by reading. No stubs: every state the ticket enumerates is implemented and asserted, including the three it names by hand.
- reviewed s6-ecs-service-overview a639a72: REPAIRED — two real defects, both invisible to a reading and found by mutation. `ecsServiceOverview` guarded the nil client but not the row, while `FormatECSServiceOverview` reads the service unguarded: the ECS panel's own `Weights` comment records that a queued rerender can find the drill state already changed, so a service tab rendering a cluster row was a panic that takes the app down, and it is now the same `overview unavailable` the nil client gets (`TestECSServiceOverviewToleratesARowWithoutAService`). And `primaryECSDeployment` exists only to pick PRIMARY out of several deployments, yet every fixture carried exactly one, so returning `Deployments[0]` passed the whole suite; `TestServiceOverviewReadsThePrimaryDeployment` lists PRIMARY second and kills it. One comment claimed 8 cells was the widest age RelTime can render, which a four-digit day count disproves — the padding is a minimum, not a cut, so the comment was corrected rather than the code. Behaviour audited by mutation, not by re-reading: 11 mutants (first-deployment-wins, events left in response order, an unanswered assignPublicIp rendered blank, a failed image fetch labelled running, a gauge drawn from an absent reading, the event prefix trimmed against the wrong name, the no-awsvpc branch removed, `awsVpcConfig`'s inner nil check dropped, the panel's row check dropped, the header not cut to the pane, the events cap removed) were each introduced and all 11 failed a test. No stubs: every field the ticket names is rendered from data the pane really holds.
- reviewed s7-s3-overview d8fc7ae, s7-ecr-overview 643c89b, s7-vpc-overview 6acb327: REPAIRED (c79f077, bb3a20e, 9c84981, f2f2d13, 6391d9a, c82ec01) — the group shipped green with four assertions that could not fail and a whole fan-out no test could reach. Behaviour audited by mutation, not by re-reading: 30 mutants across the three formatters, the two new aggregate fetches and the tab machinery (the absent public-access block read as blocked, a partial block read as blocked, the S3-default encryption sentence read as "none", the security flag rows rendered despite a failed read, the lifecycle cap removed, the tag sort reversed, all three overview headers cut 40 cells past the pane, images sorted oldest-first, undated images sorted first, the image sort dropped entirely, the image cap removed, an absent scan summary read as "clean", a zero severity count reported as a finding, an unevaluated lifecycle policy read as merely attached, scan-on-push off read as on, subnets counted by MapPublicIPOnLaunch instead of by routing, the DNS error guard dropped, on/off inverted, countsByKey's sort reversed, the endpoint service list unsorted, the IGW absence not stated, public and private subnet counts swapped, `sections.wait()` dropped from both fan-outs, both nil-client guards removed, and the bucket policy presence inverted) were each introduced; all 30 fail a test after the repairs. Two mutants that did not COMPILE on the first pass — dropping a `sort.Strings` leaves the import unused, and dropping `truncateBlock` leaves the width argument dangling — were rewritten as a reversed sort and a deliberately-too-wide cut rather than counted as kills, because an unused import proves the compiler works and nothing else. Five survived the first pass and each was a real gap: (1) the public-access flag rows were guarded on BOTH the error and the nil block, and the fixture had them nil together, so the error guard was answered by the nil check beside it — the test now carries a block AND the error, which is the same masked-guard shape this file already records. (2) and (3) `sections.wait()` and the nil-client guards were unreachable because `GetBucketOverview`/`GetVPCOverview` returned early on a nil client, which is a shape the instance overview deliberately does not have: `(&Client{}).GetInstanceOverview(…)` drives its whole fan-out because ec2.go guards each call for exactly this reason, and s3.go and vpc.go guard none of theirs. Repaired by moving the guard INSIDE the fan-out (`bucketSection`, `vpcSection`), so a client with no S3 is eleven reported sections rather than one error standing in for the pane, and both fetches now have the same nil-client test every other aggregate has. Without it a nil client was a panic in a goroutine, which takes the app down rather than the tab. (4) the subnet split was asserted on an equal fixture, so swapping public and private rendered the same line; the fixture is lopsided now. (5) `overview.PolicyPresent = policy != ""` sat inside a closure behind a concrete SDK client, and inverted it tells an audit that an ungoverned bucket carries a policy — it is `BucketPolicyAttached` now, tested directly. Two sort assertions were also strengthened before mutating rather than after: with three map keys a dropped sort still lands in order once in six runs, so the tag block now uses six keys and the AZ line's sort is asserted on `countsByKey` itself, because six AZs render past the pane and a Contains assertion cannot see a truncation. One duplication introduced by the group was removed: `staticOverviewTab` had copied the whole tab literal, so the next change to the Overview tab's key or title would have landed on one of the two; both now call `overviewTabEvery`. Handoff claims checked against the diff: every cited symbol resolves at HEAD, and the one the repair invalidated (the group note calling `staticOverviewTab` a 14-line function) is corrected in place. Numbers published twice and checked: `bucketRulesShown` (5) and `ecrImagesShown` (10) appear only in the code and in fixtures sized from the constants themselves, so a change to either cannot leave a stale expectation claiming to pass. One mutant is recorded rather than killed: swapping `staticOverviewTab` back to `overviewTab` at a panel's call site is invisible to a test, because both build a `MainTab` with the same key and title and differ only in the interval baked into a closure — the choice is a greppable call-site symbol, and getting it wrong costs API calls rather than correctness. Not covered, and stated rather than papered over: no live AWS call was made — the staging profile's SSO token is expired ("Token has expired and refresh failed") and re-authenticating is interactive — so every response shape here is carried from the tabs that already fetch it, not re-measured; and the eleven S3 subresource calls, the five VPC describes and `ListECRImages` remain unreachable from a test for the concrete-SDK-client reason, so the single line calling each is verified by reading. No stubs: every section all three tickets name is implemented and asserted, including the empty, error and missing-optional states the Definition of Done requires.
- reviewed s7-eks-overview d3e11d0, s8-adaptive-retry f3d33c5, s8-refresh-config d2f3c3a: REPAIRED (0a7216d) — the group shipped green with one cell's arithmetic unasserted and three numbers published twice with nothing pinning them. Behaviour audited by mutation, not by re-reading: 21 mutants across the EKS fan-out, the EKS formatter and the two config changes (the nil-Details guard dropped, the header cut 40 cells past the pane, the node-group sort reversed, the addon sort reversed, the tag sort reversed, the subnet-id sort reversed, an absent addon health read as healthy, version drift marked on equal instead of different, public CIDRs shown while the public endpoint is off, public-only and private-only swapped, Configuration's Version read off the describe instead of the list row, `sections.wait()` dropped, the nil-client guard made unreachable, disabled log types always "none", the scaling label's min and max swapped, the desired-node sum reading MinSize, `eksIDsLine`'s count dropped, the addon status and version columns swapped, the node-group version column dropped, an empty node-group section rendered as a bare heading, and `eksDetailsErr` ignoring the recorded fetch error) plus 8 on the retry and refresh-config changes (the cached-path RetryMode assignment dropped, the load option dropped, adaptive swapped for standard, `SetIntSetting` writing `!!str`, the metrics floor dropped, `nextSeconds` matching instead of stepping up, a ladder rung below the floor, and the PanelSeconds default dropped); all 29 fail a test after the repairs. Two mutants that did not COMPILE on the first pass were rewritten rather than counted as kills, per the rule this file already records: removing the header's `truncateBlock` leaves its width argument dangling (rewritten as a deliberately-too-wide cut) and removing the nil-client guard leaves the `errors` import unused (rewritten as an unreachable guard). One survived and was a real gap: `eksScalingLabel` renders three numbers in one cell ("4 (2-8)") and nothing asserted it, so swapping min and max produced "4 (8-2)" with the whole suite green — and it could not be caught by the render tests, because `plainEKS` collapses runs of spaces to read rows through the table's padding, which also erases which column a value sits in. It is asserted directly now, with the at-the-ceiling and scaled-to-zero cases, and both tables' row shapes are asserted whole. Two assertions were also strengthened before mutating rather than after: the throttle test's first draft used a hand-written APIError whose `ErrorFault()` returned a plain `int`, which does not satisfy `smithy.APIError` at all, so `IsErrorRetryable` answered true for a reason the test was not asking about — it is `smithy.GenericAPIError` now with a non-retryable AccessDenied case beside it; and a `cfg.Retryer != nil` guard written into `newClientFromConfig` was removed after reading the SDK's resolution order, because an explicit retryer is resolved before the mode is read and the guard protected nothing. Numbers published more than once and checked: `MetricsFloorSeconds` appears in the constant, as the metrics ladder's first rung (via the constant, so it cannot drift) and in a README sentence, and the three refresh defaults appear in `DefaultUserConfig` and in README.md's sample block — `TestReadmeRefreshSampleMatchesTheDefaults` now pins every refresh key and the floor sentence, which is the drift `lessons.md:21` records and the keybindings table already had a test for. Handoff claims checked against the diff: every cited symbol and every line reference resolves at HEAD (`vpc_panel.go:16`, `ui/overview.go:50`, `client.go:75`, `lessons.md:21`, `handoff.md:124`, `spec.md:11,12,14`), and the SDK claims were read from the installed source rather than from memory. The ticket-as-written finding is recorded rather than smoothed over: `s8-adaptive-retry` named only `baseLoadOptions`, and that alone leaves all five checked clients on `*retry.Standard` in normal operation, because the cached-credentials path never calls `LoadDefaultConfig` — the mutant proves it. Not covered, and stated rather than papered over: no live AWS call was made (`aws sts get-caller-identity` reports "Unable to locate credentials"), so every EKS response shape is carried from the tabs that already fetch it, not re-measured; `DescribeCluster`, `ListNodegroups` and `ListAddons` remain unreachable from a test for the concrete-SDK-client reason, so the single line calling each is verified by reading; and `PanelSeconds`/`MetricsInterval` have no consumer yet, which is the shape of the config half of a two-ticket split but means those two Settings rows change nothing until `s8-refresh-engine` lands. No stubs: every section all three tickets name is implemented and asserted, including the empty, error and missing-optional states the Definition of Done requires.
- reviewed s8-refresh-engine 664034b: REPAIRED (98efedd, 422116a, 6a697f9, e6a24b5) — the group shipped green with its own headline requirement asserted nowhere, and the cycle that built it was cut off before the review gate ran, leaving its first repairs uncommitted in the working tree. Those were re-verified under `-race` and landed first (98efedd): `panelSeconds: 0` had no test at all although `time.NewTicker` panics on a non-positive duration, `reloadProfilePanel` was not asserted to go through the single-flight guard, `TestReloadFocusedPanelIsInertWithoutCredentials` was passing for the wrong reason (with unnamed views the panel lookup misses and returns before the credentials guard is ever reached, so it passed whether the guard existed or not — it is headless now and proves the lookup succeeds first), the empty-memo test never tried the empty KEY, which is the case needing the timestamp check rather than the key check, and `TestGoEveryRunsOnlyWhileBackgroundThreadsAreNotPaused` leaked a ticker into every test after it. Behaviour audited by mutation, not by reading: 21 mutants across the engine, the memo and the wiring (the single-flight guard removed, the backoff not doubling, a clean fetch snapping straight to base, the ceiling removed, the gate adjusting on ticks it never fetched on, the gate pacing on `base` so the backoff never applied, `take()` never resetting its verdict, an unknown error counted as a throttle, `tickInterval` 0 no longer meaning off, the focused-panel credentials guard removed, `startAutoRefresh`'s 0-means-off guard removed, `maxAge` 0 no longer meaning never-stale, a FAILED fetch memoized, the memo answering regardless of key, `observeSections` dropping the throttle, the service memo keyed on name alone, the loaders left unguarded, the auth path bypassing the guard, the aged entry point bypassing the memo, the tab given its own memo, and the tab call site reverted to the unmemoized fetch); 18 failed a test, 2 failed only after the tests that should have caught them were written, and 1 is recorded rather than killed. THREE mutants survived the first pass and all three were the same shape, which is the shape `lessons.md:18` already records: a decision living in a loop body or a call site that every test able to observe it rebuilds for itself. (1) `singleFlight` was applied in a `for` loop inside `NewGui`, so unwrapping every loader left the whole suite green — and single-flight per reloader is this ticket's headline requirement. It is `guardedReloaders` now, a function a test calls with fake loaders, asserting both that every key comes back guarded and that each key holds its OWN guard so one slow list cannot block another panel's. (2) the auth-recovery path could be pointed back at the raw `refreshProfile` invisibly; `refresh()` with a broken token is now asserted to reach the profile list through the guard. Worth recording about the first of those: the mutant did not FAIL the test, it HUNG it, because an unguarded loader blocks on the same release the first caller holds — the assertion runs on its own goroutine behind a deadline now, since a test that reports a defect by deadlocking is worse than one that reports it by failing. One real defect was found by measurement rather than by reading, and it was another group's call site: the EC2 Metrics TAB still called `GetInstanceMetrics` on `EC2StatusSeconds` (10s) while `instanceMetricQueries` asks for a 300-second period, so it paid for thirty `GetMetricData` calls per datapoint and twenty-nine could only return what was already on screen — against spec.md:14, which is this group's own `meant:`. The handoff had recorded it as a deliberate deferral on the grounds that forcing a documented user key to a 60s floor is its own kind of lie; that reasoning holds and the repair does not do it — `EC2StatusSeconds` still sets the redraw rate, and only the fetch behind it moves onto the shared memo via `GetInstanceMetricsAged`, so opening the tab and the Overview on one instance costs one fetch instead of two. The one mutant recorded rather than killed is that same tab call site being reverted: the memo is unexported to package `aws` and a nil CloudWatch client renders the identical error either way, so no `ui` test can tell the two apart without adding an exported test-only seam, which is a worse trade than reading one line — the same concrete-SDK-client gap the last four reviews record. Handoff claims checked against the diff: every cited symbol resolves at HEAD, `retry.DefaultThrottleErrorCodes` really is 14 codes and really does carry `SlowDown`, `EC2ThrottledException`, `RequestLimitExceeded` and `ThrottlingException` as `isThrottle`'s comment claims, `PauseBackgroundThreads` really is 3 non-test sites, and `ui/profile_panel.go:103` really is where the profile switch replaces the Client. Two claims the repair invalidated are corrected in place: the `panelReloads` wiring now names `guardedReloaders`, and the two threads this review CLOSED are marked as closed rather than left reading as open. Numbers published twice and checked: `backoffCeiling` (60s) is pinned against `DefaultUserConfig().Refresh.MetricsSeconds` by a test that compares two independent sources, so neither can drift silently. Not covered, and stated rather than papered over: no live AWS call was made, so nothing here is re-measured against an account; `startAutoRefresh`'s call in `Run` is still unreachable from a test because nothing drives the real main loop. No stubs: every clause the ticket enumerates is implemented and asserted, including the single-flight drop, the backoff double/decay table and the ceiling, and `PauseBackgroundThreads` stopping and restarting a real `goEvery`.
- reviewed s9-copy-key 8b3321f, s9-footer-labels 89519e3, s9-benchmarks e5c7f74: REPAIRED (eae2c57, and one test added after) — the group shipped green with a whole screen unconsidered, two assertions that could not fail, and three numbers published from memory rather than from a run. Behaviour audited by mutation, not by re-reading: 16 mutants across the copy value, the popup path, the binding set, the footer builder, its renderer and the bench wiring (arnOrName preferring the name, the empty-value guard dropped, focus history swapped for the focused view, the main binding dropped, ECS copying the row name, the ordered renderer sorting its parts, the labels hardcoding default keycaps, the filter label ignoring IsFilterDisabled, the whole line stopping being contextual, renderGlobalOptions pinned to one view, the new bench packages dropped from the Makefile, `main` dropped from resourceViewNames, the chat-screen copy guard removed, the chat-screen footer branch removed, ECS copying the cluster ARN at every level, and the task level copying the task ID); 14 fail a test, 1 was recorded rather than counted, and the rest only failed after the tests that should have caught them were written. FOUR mutants survived the first pass and every one was a real gap. (1) the ordered renderer could `sort.Strings` its parts and the whole footer reordered with the suite green, because the order was asserted on the slice HANDED to the renderer rather than on the line it produced — one step too early is the same as not asserting it. (2) `renderGlobalOptions` could be pinned to `dashboardOptions("ec2")` and every per-view assertion still passed: the argument is a call site, and no test of the builder can see it, which is the shape lessons.md:18 and :38 already record twice. It is now driven through `renderPanelOptions` with focus really moved. (3) the filter label's guard had no coverage in either direction, because every shipped list is filterable; a real panel is toggled now rather than the guard being deleted, since the label and the BINDING answer to the same condition and a future unfilterable panel would otherwise advertise a key that does nothing. (4) `main` could be dropped from `resourceViewNames` invisibly, because both the copy-binding test and the footer's vocabulary test took their expected view list FROM that function — the copy key and its label would have disappeared from the detail pane with nothing red. Expectations are built independently now, main is named on purpose, and the function has its own test. One real defect was found by reading the code around the change rather than the change itself, and it is the one that would have reached a user: the chat screen hands focus to main (`handleQFocusNext`: qChats → main) with the eight lists hidden, and BOTH new features are bound there, so `y` reported the identifier of whatever the dashboard had selected before the user left it, and the footer advertised copy, tabs, select and actions, none of which do anything in a conversation. `handleCopySelected` now takes the `mainBelongsToQ()` guard the render paths already use, and main-in-chat gets its own four-entry line (scroll, next pane, dashboard, quit), each entry a key really bound there. NOT touched, and recorded instead: `activeMainRows` has no such guard, so `a` in the chat opens the dashboard's actions menu — that is another group's code and this review does not reach into it. One mutant is recorded rather than killed: reading the cluster ARN when `row.Cluster != nil` is indistinguishable from `row.arn()` for every state the app can build, because `fetchECSRows` sets exactly one payload per row kind, so it is an unreachable difference and not a kill; a reachable substitute (the task level copying the task ID) fails the drill-level test that was added for it. Numbers published more than once and checked against a run: the footer's worst case is 87 cells and not the 86 the handoff and the s9-footer-labels commit message claimed (measured before the trimming pass, published after), the popup test's ARN is 76 characters and not 78, and the group added 14 benchmarks and not 13 — the handoff is corrected at every site and records that the commit message keeps the wrong number. The benchmark figures themselves come from an actual `-bench` run and every one is inside the planned budget (fit table 94µs against a 150µs budget and 1.03x RenderTable against a 2x ceiling, Columns 13µs against 100µs, formatters 15-31µs against 1ms, the 100-instance rerender 138µs against 500µs). Handoff claims checked against the diff: every cited symbol resolves at HEAD (`handleCopySelected`, `arnOrName`, `SelectedCopyValue`, `CopyValue` on all eight constructors, `KeyCopyID`, `resourceViewNames`, `dashboardOptions`, `optionsToString`, `sidePanelNamed`), `make keys` really did regenerate the README row, and the claim that a global `y` would be shadowed was verified against `confirmation_panel.go:156` and `keybindings.go:180`, which both bind it. Not covered, and stated rather than papered over: no TUI was run (this cycle has no tty), so the footer is verified as the string the options view holds and not by eye; no live AWS call was made, though this group reads no response shape it did not already have; and the popup's own esc/y handling is the confirmation panel's existing behaviour, untouched and untested here. No stubs: every clause of all three tickets is implemented, including the eight copy values, the per-panel labels, and a benchmark for the fit table, both column layouts, all eight overview formatters and the list rerender.
- reviewed s10-ui-harness 628c034 cf196b9: REPAIRED (5271823) — the harness shipped green while perturbing the app it was built to observe, and the first repair's own test could not fail. One real defect, found by reading the code the harness touches rather than the harness itself: `openTerminal` focused the terminal by clicking it, which is a REAL mouse event — gocui sets `g.Mouse` from `IgnoreMouseEvents` (default off, so tracking is on), `keybindings.go:222` binds MouseLeft on `main` to `handleQConversationClick` → `handleMainClick` → `switchFocus(Views.Main)`, and the click at the page centre lands in the detail pane at 160 cols. Measured rather than argued: the footer read `←→↑↓ navigate, enter switch, …` (the profile panel's) before the click and `←→↑↓ scroll, [ ] tabs, enter select, …` (main's) after it, so every journey in `s10-ui-journeys-panels` and `-keys` would have started from the wrong focus. It only LOOKED harmless because the click raced startup and was usually swallowed, which is worse than a consistent bug. Focus goes through `window.term.focus()` now, which reaches the pty with no event, and keys still arrive (`4` moved the footer to S3's `enter inspect`). The first attempt at pinning it did NOT work and the mutation is what said so: with the assertion added and the click restored, the journey still passed, because the click was swallowed before the app had drawn. `openTerminal` now waits for the options bar (`q quit`, on every screen this app draws) before focusing, so a journey never reads a half-drawn screen AND the click mutant lands on a live app — restored, it fails with `focus after boot: expected enter switch`. Behaviour audited by mutation, not by reading: 6 mutants (the focus click restored, the ECR seed step dropped, the endpoint test built from a hand-assembled config instead of the loaded one, the harness profile renamed out from under `AWS_PROFILE`, a journey that throws, and the dependency check with a bare `PATH`) were each introduced and all 6 failed — the seeded-ECR one as `ECR panel: expected lazyaws/api`, the profile one as a 15-second `waitForText` timeout carrying the whole screen, the failing journey as `EXIT=1` with `zzfail-failure.png` written beside `smoke.png`, and the bare `PATH` as `make ui-test needs: docker ttyd aws bun or node` with exit 1. One mutant is equivalent and measured rather than assumed: `readScreen` reading from index 0 instead of `buffer.viewportY` is byte-identical, because gocui draws through tcell's alternate screen and no scrollback accumulates (`viewportY: 0, bufLength: 47, rows: 47` after focusing a panel and arrowing down twice). `viewportY` is kept because it is correct by construction, not because a test proves it. One HOME-isolation mutant is recorded rather than run: pointing the app at the operator's real `~/.aws` is blocked in this environment, so the same invariant was mutated from the other side — renaming the profile the harness writes — which is what killed it. Two more defects were found by reading and repaired here: ttyd failing to bind its port left Playwright to report a refused connection with nothing naming the cause, and the harness profile was written in two places (`AWS_PROFILE` and the config heredoc) with nothing pinning them, which is the numbers-published-twice shape this file already records — one `profile=` variable now feeds both. Handoff claims checked against the diff: the cited symbols and files all resolve at HEAD, and the two the repair invalidated are corrected in place (the helper list gained `footer` and the boot wait, and the `verified:` line no longer defers the runner's failure path to the review). Not covered, and stated rather than papered over: no real AWS call was made and none should be — every shape here is moto's, which is the point of the ticket; `make ui-test` is not in the per-cycle gate (spec.md:43), so nothing in `make lint test` runs any of this except the endpoint contract; and the three moto limits the handoff records (null cluster statistics, one container per task, a default VPC in the list) are the emulator's, not the app's, and constrain what the next three journey tickets can assert. No stubs: the target, the seeded fixtures the ticket enumerates, the four helpers it names, the non-zero exit and the smoke journey are all implemented.
- reviewed s10-ui-journeys-panels 16ee5c3: CLEAN — mutants killed: EC2 `(no name)` fallback renamed, `NoItemsMessage` emptied
- reviewed s10-ui-journeys-overview f92a213: REPAIRED (b649378) — the Overview HEADER was asserted with a screen-wide substring check, which cannot fail: "VPC" is also the side panel's frame title and "Instance" sits inside the ECS pane's "Instances" tab. Headers now go through the whole-pane-cell check the sections already used, proved by a mutant renaming the VPC overview's header. Mutants killed: Overview tab moved out of first place, minTwoColWidth 110 → 20
- reviewed s10-ui-journeys-keys a6a9e62: REPAIRED (b649378) — the copy popup's assertion looked for the full instance id ANYWHERE on screen, and the detail pane's header prints exactly that id, so a mutant publishing the wrong copy value survived. It now reads the popup's own lines between its own borders and kills that mutant. Mutant killed: `handleRefreshAll` reduced to the focused panel, which `make test` did not catch
