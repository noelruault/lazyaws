# Handoff — TuiRedesign

One block per shipped ticket, appended by the builder before the review gate runs. Pointers into the diff, not a narrative. Read the index first (`bash scripts/docs-slice.sh index handoff`), then slice only the ids you need.

### s1-cell-rendertablefit (a0bcb37)

meant: aligned columns that survive a long value, spec.md:10.
did: `ui/utils/utils.go` — `Cell{Text, Color}` with `Cell.render`, `RenderTableFit`, `fitColumnWidths`. Weight 0 sizes to widest cell, weight >0 shares the remaining budget, last flexible column takes the rounding remainder so the table fills the width exactly. `RenderTable` untouched.
deviations: `Cell.Color` is a `color.Attribute`, not a `*color.Color` — an Attribute is what `utils.ColoredString` takes, so the precomputed-escape path is kept and no Color is allocated per cell; the zero value therefore has to mean "unstyled" (`color.Reset` as a cell style is meaningless) and is documented on the field. Validation is wider than the ticket asked: also errors on a weights slice that does not match the column count, and on a negative weight, because both would silently mis-lay-out every row. Degenerate case the ticket did not specify — when content-sized columns alone overrun the terminal, the budget is spent left to right and the trailing columns are squeezed to zero; chosen because the leftmost columns carry the identifying text. No benchmark: `s9-benchmarks` owns it.
verified: `make lint`, `make test` (both exit 0). Exact-string tests in `ui/utils/utils_test.go` (`TestRenderTableFit*`), plus `TestRenderTableFitNeverExceedsTheWidth` sweeping every width 0-45 across each weight shape, wide runes and colour. Green covers the width arithmetic, the CJK cut, the remainder, and that escapes wrap the truncated text; it does not cover anything on screen, since nothing calls this yet.
threads: `runewidth.Truncate` subtracts the tail's width from the budget, so at width 0 it returns a one-cell `…` and overruns the column — `Cell.render` guards width <= 0 for that reason, do not remove it. Cell.Text must be plain: passing pre-coloured text defeats the whole design and mismeasures the column.

### s1-primitives (8c9f314)

meant: semantic status badges, icon plus word, never colour alone, spec.md:10.
did: `ui/presentation/primitives.go` — `Badge`, `Gauge`, `RelTime` (+ `compactDuration`), `SectionTitle`, `ResourceHeader`, `FormatByteCount`. `ui/presentation/status.go` gains `statusKindOf`, now shared with `StatusCell`. `ui/ec2_panel.go:267` `formatByteCount` is a one-line alias over `presentation.FormatByteCount`.
deviations: `Badge` uses the icon from `statusStyleTable` (`▶ ⨯ ⟳ − !`), so it does NOT render the `●` the mockups show — the ticket says "through the existing statusStyleTable aliases" and that is the table's icon. A later ticket wanting `●` changes the table, not Badge. Three guards the ticket did not ask for, each preventing a real failure rather than adding scope: `Gauge` clamps NaN (a CloudWatch metric with no datapoints arrives NaN, NaN-to-int is undefined in Go, the bar length goes negative and `strings.Repeat` panics), `RelTime` reports the zero value as `unknown` (an absent AWS timestamp would otherwise be related against year 1) and renders future times as `in 7d` (rotation dates are in the future; without it the output is a negative duration). Small refactor outside the three new files: `statusKindOf` extracted from `StatusCell` so Badge cannot drift from it — the alternative was duplicating the alias lookup. `FormatByteCount` moved verbatim, including its unbounded `"KMGTPE"[exp]` index; not touched, so the move changes no behaviour.
verified: `make lint`, `make test` (both exit 0). Exact-string tests per primitive in `primitives_test.go`, colour asserted under a local `forceColor` helper. `ui/ec2_panel_test.go` still exercises the alias unchanged, which is what proves the move was behaviour-preserving.
threads: `color.Faint` (the muted attribute, used by `ResourceHeader` and wanted by `s2-inpanel-empty`) is NOT in `utils.colorWraps`, so it takes the un-precomputed fallback. Correct output, one allocation per call; left alone because nothing measures it yet. `ResourceHeader` always emits three lines, even with no meta, so sections below it do not shift — a caller that trims the trailing newline breaks that.

### s1-columns (50e891f)

meant: two-column overviews that collapse to one column on narrow terminals and never soft-wrap, spec.md:12.
did: `ui/presentation/columns.go` — `Columns`, `truncateStyled`, `csiPrefixLen`, `minTwoColWidth = 110`. Splits both blocks on newlines, cuts each line to `(width - 2*gap - 1) / 2`, pads the left with `utils.WithPadding`, joins with a `│` rule; stacks below the threshold.
deviations: the ticket says "ANSI-aware measurement via Decolorise", which taken literally means decolorising before the cut and losing the line's colour exactly when it overflows. Implemented as escape-preserving truncation instead (`truncateStyled` copies CSI sequences through, counts only visible cells, closes the cut with a reset) — measurement is still Decolorise-based, the colour just survives. `gap` is defined as the blank cells on EACH side of the rule, so the rule consumes `2*gap + 1`; the ticket did not say which. Two policies it did not specify: an empty right block returns the left block untouched, and a gap wide enough to leave no column falls back to stacked instead of emitting negative padding. Width is counted per rune, not per grapheme cluster, so a combining mark would be double-counted — noted in the comment, no AWS identifier carries one. No benchmark: `s9-benchmarks` owns it.
verified: `make lint`, `make test` (both exit 0). `columns_test.go` covers ragged heights, coloured measurement, the narrow fallback and the threshold itself, empty blocks, plain and coloured truncation, plus `truncateStyled`/`csiPrefixLen` directly, and `TestColumnsNeverExceedsTheWidth` sweeps every width up to 260 across four gaps. Green proves the string arithmetic; it proves nothing about how 110 looks on a real terminal.
threads: `minTwoColWidth = 110` is a starting guess to be tuned by eye, so the tests deliberately use width 200 and only the threshold test references the constant — tuning it must not rewrite every expectation. `csiPrefixLen` handles CSI only, matching what `utils.Decolorise`'s regexp matches and what fatih/color emits; an OSC sequence would be counted as text.

### Group notes (s1-cell-rendertablefit, s1-primitives, s1-columns)

Not delegated. Three tickets is under the size where a router plan pays for itself, and the exact-string expectations could not be specified in advance — they fell out of design calls made while writing the code (weight arithmetic, separator widths, whether truncation preserves colour). Writing a plan precise enough for a cheap model would have meant deciding all of it first anyway.

Connected AWS verification: not applicable. Stage 1 is pure additions with nothing wired to a view, so there is no surface to eyeball; no profile was used and no AWS call was made.

### s2-inpanel-empty (21c5126)

meant: in-panel empty states, spec.md:10.
did: `ui/panels/side_list_panel.go` — `emptyMessage`, `afterRerender`; `RerenderList` branches on an empty list before building a table. The eight panels' `NoItemsMessage` values lose their trailing "found".
deviations: profiles reads `no AWS profiles in ~/.aws/config`, not the ticket's bare style — the path is the only actionable thing the message says, and with no profiles there is no row to select to reach the Config tab that would otherwise tell you. `emptyMessage` is a method rather than an inline `ColoredString` call purely so the muting is testable; gocui parses escapes into cell attributes, so `View.Buffer()` returns the text stripped and no view assertion can see `color.Faint`. `afterRerender` is an extraction, not new behaviour: the empty branch owes `OnRerender` and the focused-view `HandleSelect` exactly what the table branch owes them.
verified: `make lint`, `make test` (both exit 0). `ui/eks_panel_test.go` headless: the message reaches the EKS view buffer, and disappears once a cluster is listed. `ui/panels/side_list_panel_test.go` pure: the muting, and the menu panel's empty message staying empty rather than becoming a bare escape pair. Four mutants (empty branch removed, message unmuted, blank-message guard dropped, message shown alongside rows) each failed a test.
threads: the blank-message test only bites under `forceColor` — a test binary is not a tty, so without it `ColoredString` returns plain and the guard's mutant survives. That is why `forceColor` now exists in `ui/panels` too.

### s2-left-profiles-ecs (a1c3d7f)

meant: aligned columns and semantic status badges on the left panels, spec.md:10.
did: `ui/panels/side_list_panel.go` — `GetTableCellsFit`, `Weights`, `renderTable`, `searchCells`. `ui/presentation/status.go` — `StatusCellFit`, `GetProfileDisplayCells`. `ui/presentation/ecs.go` — `GetECSClusterDisplayCells` (+ `ecsClusterBadge`), service and task cell rows, three weight functions. `ui/utils/utils.go` — `Cell.Rendered`.
deviations: the ticket scopes the ECS work to cluster rows, but services and tasks had to move too — one panel has one render path, and `RenderTableFit` rejects ragged rows. Those two are a mechanical 1:1 conversion, same content and colours; only clusters gain the badge. `Weights` is `func(T) []int` rather than a slice because the ECS panel is three tables depending on drill level; taking the first row means the weights always describe the rows actually being rendered, where reading `gui.ecsDrill` could describe a level a queued rerender has already left. The badge reads `● healthy` / `● deploying` / `● <status>`, not `Badge()` from s1-primitives, which renders the `statusStyleTable` icon and the raw status word — the ticket specifies this text. Only an ACTIVE cluster is ever `deploying`; the ticket's literal ordering would call a cluster that is being deleted with tasks still draining "deploying", which reads as progress. An absent status renders `● unknown` rather than a bare `● `. Filtering changed behaviour: it now matches `Cell.Text`, where before it matched rendered strings carrying colour escapes, so a filter could not hit a coloured column. `GetECSServiceDisplayStrings` survives as a thin `Rendered()` projection because the cluster inspector's services table still uses `RenderTable`; its pre-existing test passes unchanged, which is what shows the conversion preserved behaviour.
verified: `make lint`, `make test` (both exit 0). Exact-cell tests per cluster state in `ui/presentation/ecs_test.go` including empty/INACTIVE/DEPROVISIONING/absent-status, profiles in `status_test.go`, filter and `SelectByCell` over a migrated panel in `ui/panels/side_list_panel_test.go`, and a headless `TestECSPanelRendersClusterRowsInsideTheViewWidth` that resizes the ECS view to 60 and asserts no rendered line exceeds it. 11 mutants killed. Green does not cover how any of it looks on a real terminal.
threads: `Cell.Rendered` exists because `ColoredString(text, 0)` wraps text in a reset pair — the zero attribute means unstyled to `Cell` and `color.Reset` to fatih/color. That bug was live in the first draft of the services-table conversion and the test caught it; every string-form conversion must go through `Rendered`, never `ColoredString(cell.Text, cell.Color)`. The headless harness leaves views at the 10-cell placeholder `createAllViews` sets (`InnerWidth()` = 8), so any view-level row assertion has to resize first — `resizeView` in `ui/ecs_panel_test.go` does it and restores the size on cleanup, which matters because the gocui screen is one process-wide global.

### s2-left-ec2-s3 (d30c315)

meant: prominent name, muted identifiers, aligned columns, spec.md:10.
did: `ui/presentation/ec2.go` — `GetInstanceDisplayCells`, `InstanceWeights`. `ui/presentation/s3.go` — `GetBucketDisplayCells`, `BucketWeights`. Both panels switched to `GetTableCellsFit`.
deviations: the instance id column is flexible (weight 1 against the name's 2), where the ticket implies only the name flexes. The ticket's own mandated case — a 60-char name at width 40 — is what forced it: icon, id, type and private IP are 36 cells of content plus 4 separators, so the flexible name column resolved to zero width and the name vanished while four narrower columns survived. Both text columns now share the squeeze. The status icon stays as the first cell; the ticket lists four fields but removing the icon would drop information, which spec.md:11 forbids.
verified: `make lint`, `make test` (both exit 0). `ui/presentation/ec2_test.go` and `s3_test.go`: exact cells named and unnamed, the 60-char name at width 40 asserting total width, the ellipsis, the name surviving and the other columns still readable, that colour is applied after the cut, and that each weight table matches its row width. 7 mutants killed.
threads: the same narrow-width squeeze applies to the ECS cluster row, whose five columns are ~44 cells of content — see the review line for what was done about it.

### Group notes (s2-inpanel-empty, s2-left-profiles-ecs, s2-left-ec2-s3)

Not delegated. The bulk here looked mechanical — four panels, one conversion each — but it was not specifiable in advance: the shared `GetTableCellsFit`/`Weights` seam had to be designed against the ECS panel's shifting row shape before any panel could move, and the weight tables were decided by measuring rendered rows across widths 30-100, not by a rule anyone could have written into a plan. The one genuinely mechanical part, converting the string formatters to cells, is under a hundred lines; a router plan precise enough to hand it down would have cost more than it saved.

Connected AWS verification: not run. Nothing here depends on a response shape — every change is downstream of structs the repo already populates — and the surfaces worth eyeballing (a narrow panel, an empty panel, a badge) are exercised by the width sweeps and the headless view tests instead. No profile was used and no AWS call was made, so the "how does it actually look at 40 cells" question is answered by measurement, not by sight; `s10-ui-journeys-panels` is where that gets seen.

### s2-left-eks-ecr (03f6ea4)

meant: prominent name, muted identifiers, aligned columns, semantic status badges of icon plus word, spec.md:10.
did: `ui/presentation/eks.go` — `GetEKSClusterDisplayCells`, `EKSClusterWeights`, `createdDate`. `ui/presentation/ecr.go` — `GetECRRepositoryDisplayCells`, `ECRRepositoryWeights`, `ecrMutabilityBadge`. `ui/presentation/primitives.go` — `BadgeCell`, with `Badge` now a `Rendered()` projection of it. Both panels switched to `GetTableCellsFit`/`Weights`; the string formatters they called are deleted, along with `GetECRRepositoryDisplayStrings`.
deviations: the EKS row keeps the status WORD next to the icon (`▶ ACTIVE`) instead of the icon-only cell EC2 uses, because EKS reports UPDATING, which `stateAliases` has no entry for, and `ListEKSClusters` writes the literal "unknown" for a cluster whose describe failed — icon-only renders both as the same bare `?`. The EKS creation stamp is cut to its date: at the 30-60 cells a side panel really gets, the full 19-cell stamp is truncated to `2026-01-02 1…`, which reads as a broken value rather than as a time; the whole stamp is still on the Config tab (`ui/eks_panel.go:143`). Two content changes the ticket did not ask for, both making a headerless column say what it is: the node count reads `12 nodes` (matching the shipped `N services` on the ECS row) and the scan column reads `scan on`/`scan off` rather than a bare `on`/`off` next to a mutability badge. ECR colours ONLY the mutable case: an immutable repository is the posture nobody needs alerting to, and amber on both spends attention on the safe one. Mutability matches on the prefix because the enum has four values — `MUTABLE_WITH_EXCLUSION` and `IMMUTABLE_WITH_EXCLUSION` carry an exclusion list, so they render starred (`immutable*`) rather than claiming a blanket policy; an unrecognised value is passed through raw. Not a deviation but worth stating: `ui/eks_panel_test.go:97` had to gain `resizeView`, since the fit path renders at the headless 8-cell placeholder and cut the asserted row to `▶ AC…`.
verified: `make lint`, `make test` (both exit 0). `ui/presentation/eks_test.go` and `ecr_test.go`: exact cells per state (ACTIVE, an unaliased UPDATING, the client's "unknown", DELETING; immutable, mutable, both exclusion variants, no policy, an unknown value), `createdDate` including a stamp with no space in it, both weight tables against their row width, and a 40-cell render asserting the total width, the name surviving, and `12 nodes`/`2026-01-02`/`● mutable`/`scan on` still whole. Green covers the strings and the arithmetic; nothing here has been seen on a terminal.
threads: at width 30 the EKS date column is squeezed to a lone `…` — the trailing column pays for the left-to-right clamp once the badge, name floor and node count are paid. Measured, accepted: it degrades to an ellipsis rather than to a wrong date, and the columns that identify the row survive. `BadgeCell` is the cell twin of `Badge`; anything rendering a status into a fit table must use it, because `Badge` returns coloured text and `Cell.Text` must stay plain to be measured.

### s2-left-secrets (401c10f)

meant: prominent name, muted identifiers, glanceable rows, spec.md:10.
did: `apps/aws/secrets.go` — `SecretSummary.RotationDays` plus the nil-safe `rotationDays`, filled by both `ListSecrets` and `GetSecretDetails`. `ui/presentation/secrets.go` — `GetSecretDisplayCells`, `SecretWeights`, `rotationCell`; `GetSecretDisplayStrings` deleted. `ui/secrets_panel.go` switched to the fit path.
deviations: a third rotation state the ticket's two did not cover. `RotationRulesType.AutomaticallyAfterDays` is `*int64` and is absent twice over — a secret that never had rotation configured carries no `RotationRules` at all, and the SDK's own doc says that on `ListSecrets` the field is calculated from the schedule after every successful rotation, so a secret scheduled by `cron()`/`rate()` has rotation ON with no day cadence. That renders `rotation on`; `rotation 0d` would be a cadence nobody configured. The column labels its own value (`rotation 7d`, not `7d`) because a bare `7d` beside a secret name reads as an age. `GetSecretDetails` fills the same field although the ticket named only `ListSecrets`, so a `SecretSummary` means the same thing whichever call produced it. The pending-deletion column is coloured red but otherwise left as it was, including its `-` placeholder on every live secret.
verified: `make lint`, `make test` (both exit 0). `ui/presentation/secrets_test.go`: exact cells for no rotation, 7d, rotation-on-without-cadence, pending deletion, and a rotating secret pending deletion (the two columns are independent), plus a 40-cell render keeping the cadence whole. `apps/aws/secrets_test.go` `TestRotationDaysIsNilSafe` covers nil rules, rules with no cadence, and 7 days.
threads: the mapping itself is NOT covered end to end — `Client.Secrets` is a concrete `*secretsmanager.Client` with no interface seam, so no test can drive `ListSecrets` with a fake response; the pure `rotationDays` is what carries the assertion, and the `RotationDays: rotationDays(...)` wiring in both call sites is unasserted. A seam is the only way to close that and it belongs to no ticket here.

### s2-left-vpc (8ce1d70)

meant: prominent name, muted identifiers, aligned columns, spec.md:10.
did: `ui/presentation/vpc.go` — `GetVPCDisplayCells`, `VPCWeights`; `GetVPCDisplayStrings` deleted, `GetVPCEndpointDisplayStrings` untouched. `ui/vpc_panel.go` switched to the fit path.
deviations: the CIDR is the bold column, not the name — the row already led with it, and the pre-existing reason holds (a VPC is recognised by its network when tracing reachability, and the name tag is often absent). The vpc id is sized to its CONTENT rather than given a share of the slack, where EC2's id flexes: a vpc id is a fixed 21 cells, so a proportional column under-paid it and cut it even at width 60 with 28 cells idle in the label's padding. Measured across 30/40/50/60 before and after; the label's `flexibleFloor` is what keeps the row readable at 30-40, where the id is the column that gives way.
verified: `make lint`, `make test` (both exit 0). `ui/presentation/vpc_test.go`: exact cells, the default-VPC marker on an unnamed VPC, a 40-cell render asserting total width and that the whole CIDR survives (a truncated network address is a different network), and `TestVPCRowShowsTheWholeIDOnceThePanelHasRoom` pinning the weight decision at 60.
threads: `presentation.GetVPCEndpointDisplayStrings` has NO production caller — the endpoints tab builds its own rows in `ui/vpc_panel.go:vpcEndpointRowCells`. Pre-existing dead code with a live test, left alone because it is outside this group.

### Group notes (s2-left-eks-ecr, s2-left-secrets, s2-left-vpc)

Not delegated. Four panels of conversion looks like bulk, but each decision that mattered was a judgement made against measured output: whether EKS keeps its status word (it does, because two distinct states collapse to `?` without it), what an exclusion-carrying ECR policy may claim, what a rotation with no day cadence says, and which VPC column gives way at 40 cells. The mechanical remainder is small — 172 production insertions against 48 deletions across ten files, most of it the four formatters themselves; a plan precise enough for a cheap model would have had to state all four answers first, which is the whole of the work.

Connected AWS verification: not run, no profile used and no AWS call made. Two response shapes this group depends on were verified against the installed SDK source instead of assumed: `SecretListEntry.RotationRules *RotationRulesType` with `AutomaticallyAfterDays *int64` (and the doc sentence that makes it absent on expression-scheduled secrets), and the four-value `ImageTagMutability` enum, which is why the badge matches on a prefix. Row appearance is answered by rendering at 30/40/50/60 and reading the output, not by sight on a terminal.

### s3-selection-identity (88b0aac)

meant: spec.md:15 (selection preserved by resource identity, never by index; the detail pane must never describe a different resource than the visibly selected row).
did: `ui/panels/side_list_panel.go:SetItemsKeepSelection` reads the selected item's key before the swap, calls `SetItems`, then re-finds that key in the filtered+sorted list; all 8 loaders pass a key (`ui/ec2_panel.go`, `s3_panel.go`, `eks_panel.go`, `ecr_panel.go`, `secrets_panel.go`, `vpc_panel.go`, `ecs_panel.go` `row.arn()`, `ui/profile_panel.go:refreshProfile`).
did: `refreshProfile` also lost its hand-rolled find loop to the existing `SelectByItem`, and its jump to the connected profile is now gated on the first load.
deviations: the key is a parameter and not `ContextState.GetItemContextCacheKey`, which already exists per panel and looks like the same thing — it is not, it deliberately mixes in mutable state (`ui/secrets_panel.go:52` folds in whether the value is revealed) so matching on it would be matching on a cache key. Behaviour change beyond the ticket's words: `refreshProfile` is a reloader, so pinning the connected profile on every call yanked the cursor off whatever row the user moved to; the pin now runs only when the list was empty. `ecs_panel.go:drillECS` still calls plain `SetItems(nil)` on purpose, so a drill in or out starts at row 0. Not wired: `ui/s3_objects.go` and `menu_panel.go` are not among the 8 resource panels.
verified: `make lint` and `make test` from the repo root, both exit 0. Green covers the method's own table (reorder, top-move, state change, insertion above, disappearance, empty reload, empty key, filtered list), the EC2 reorder through the panel's real running-first sort, the profile first-load-then-leave-alone sequence on the headless screen, and every panel's key function against a fixture where the identifying field and the displayed one differ. Green does NOT cover the loader functions themselves: there is no fake AWS client in `ui/`, so `loadEC2List` and its seven siblings are unreachable from a test, and what pins their keys is that the loader and the test name the same symbol (`ec2SelectionKey` and co.) rather than each writing its own closure. The review below is why they are named.
threads: an ECS drill level change reuses one panel, so `row.arn()` is unique per level in practice but the level is not part of the key; harmless today because `drillECS` clears the list first. Stage 8's single-flight reload (`s8-refresh-engine`) is the first caller that will hit this method concurrently — the method is called on the UI loop today and is not synchronised.

### s3-selection-theme (88b0aac) — VOID, nothing built

meant: the ticket asked for a `SelectedLineFgColor` theme key applied as `SelFgColor` where `SelBgColor` is set, plus a Settings row. No spec.md line requires it; spec.md:17's quality bar is the closest thing.
did: nothing in the app. `docs/tui-redesign/backlog.md:24` carries the evidence and the revival condition; `built.md` carries the VOID line because both the selector and the DoD read that file by id.
deviations: THE WHOLE TICKET. It was closed instead of built, and it was closed rather than left open because the selector would then re-dispatch it alone every cycle (`s4-overview-machinery` is HUGE, so the batch stops before it) and no cycle would ever reach stage 4. `gocui v0.3.1-0.20240418080333`'s `setRune` highlight branch forces the bright fg variant plus `AttrBold` and ORs `SelBgColor`; it never reads `View.SelFgColor`, which is used only for tab titles, and the side panels have no tabs. Reopen it in one edit if that call is wrong.
verified: not by reading the source only — a throwaway probe set `SelFgColor = ColorRed` on a highlighted headless view and read the drawn cell off the tcell simulation screen: `fg=default bg=navy bold=true` on the selected line, `fg=default bg=default bold=false` on the line below. The probe was deleted; it needed `gdamore/tcell` as a direct import to read the screen, which would have promoted an indirect dependency to keep a test that asserts upstream behaviour.
threads: the same probe technique is the only way to assert drawn colour in this app (`View.Buffer()` is stripped of attributes), so any future ticket about selection rendering has a method available but pays a `tcell` direct import for it. Bold is currently forced onto EVERY cell of the selected row, which means the bold name column stage 2 added is not distinguishable there — cosmetic, unticketed, and not repaired here.

### Group notes (s3-selection-theme, s3-selection-identity)

Not delegated. Two members, one of which turned out to be a contract question about a dependency's draw path — writing a plan for that would have cost more than the delegation saved, and the mechanical half was seven one-line call-site edits.

Connected AWS verification: not run, no profile used and no AWS call made. Nothing in this group reads an AWS response shape; the keys used (`Instance.ID`, `Bucket.Name`, `EKSCluster.Name`, `ECRRepository.Name`, `SecretSummary.Name`, `VPC.ID`, `ECSTask/Service/Cluster.Arn`) are fields the panels already render.

### s4-overview-machinery (74fbfe3)

meant: spec.md:11 (an `Overview` tab per resource, first in the tab list, existing tabs and keys preserved), spec.md:12 (width-aware, wrap off) and the overview half of spec.md:14 (tiered auto-refresh). Content is out of scope here; each resource's own stage 5-7 ticket fills it.
did: `ui/overview.go` — `overviewTab` builds the tab, reading `Views.Main.InnerWidth()` inside `Render` (which `SideListPanel.renderContext` calls on the UI loop) and closing over it, so the render function never touches a gocui view from the task goroutine. `newOverviewTask` returns a `NewTickerTask` with `Wrap:false`, or a one-shot `NewTask` when the interval is 0. `renderOverview` snapshots `gui.Gen` before the render runs and drops the result if it moved.
did: `ui/layout.go:60` calls the new `syncMainWidth`, which records main's inner width and triggers `gui.rerenderMainTab` (a 50ms `throttle`, `ui/gui.go:179`) when it moves; `rerenderCurrentMainTab` clears `State.Panels.Main.ObjectKey` so `ShouldRefresh` lets the re-render through, and returns early on `mainBelongsToQ`. `config.RefreshConfig.OverviewSeconds` added, default 2.
did: seven registries prepend the tab (`ui/ec2_panel.go:22`, `s3_panel.go:23`, `eks_panel.go:71`, `ecr_panel.go:21`, `secrets_panel.go:48`, `vpc_panel.go:23`, and `ecs_panel.go` at the cluster and service drill levels).
deviations: the tab's CONTENT is a stated `<resource> overview unavailable`, not a layout — the ticket is plumbing and stages 5-7 own each formatter, but that means the branch currently opens every resource on an empty first tab. Two registries deliberately do NOT get the tab: the profile panel (a profile is not an AWS resource) and the ECS task drill level (its detail is the task definition); neither has a stage 5-7 overview ticket, so a tab there would be a first tab nothing ever fills. `OverviewSeconds` gets no Settings screen row — the settings model (`ui/settings_panel.go:34`) has only bool and choice kinds, and adding a numeric kind is scope `s8-refresh-config` will have to pay anyway. `overviewInterval` folds a negative to 0 rather than to the default, because `time.NewTicker` panics on a non-positive duration and `config.RefreshInterval` (which substitutes a fallback) would make 0 mean "2s" instead of "off". `TestECSServiceTabsCycle` was edited, not left unmodified: it pins tab ORDER, which this ticket changes by contract.
verified: `make lint` and `make test` from the repo root, both exit 0. Green covers the interval table, one-shot vs ticking behaviour over a real 15ms ticker, the generation guard (the superseded render is polled for 150ms and never reaches main), the width actually handed to a render being `Views.Main.InnerWidth()` after a resize, `syncMainWidth` firing once per width CHANGE and not per layout pass, `rerenderCurrentMainTab` clearing the object key, and every wired registry rendering its own first tab end to end through the real `MainTab.Render`. Corrected after the review: this line first claimed the `mainBelongsToQ` early return was uncovered and said nothing about `Wrap:false`; both were genuine gaps that two surviving mutants exposed, and both now have tests, so green also covers wrapping being turned off on either task path and a resize leaving the chat screen holding main. Green does NOT cover: the layout call site itself (no test drives a terminal resize through `gui.layout`) or how an overview looks on a terminal.
threads: `README.md:153`'s config sample listed two of the three refresh keys once this one shipped; the review added `overviewSeconds` to it, and nothing pins that sample against `DefaultUserConfig`, so the next config key can go stale the same way. The `main` view is a process-wide global in the headless harness, so the per-registry render subtests run sequentially against one view on purpose; a parallel subtest there would read another case's buffer. `s8-refresh-engine` inherits an open question this ticket did not answer: `newOverviewTask`'s ticker has no single-flight guard, so a fetch slower than the interval will overlap itself. `overviewInterval` is the seam a per-panel or metrics-specific interval would extend. The one-shot path renders through `reRenderStringMain` and never calls `clearMainView`, which is only safe because `setViewContent` clears the view itself.

### Group notes (s4-overview-machinery)

Not delegated, and it did not look delegable: the ticket is one HUGE member dispatched alone, and every decision in it was a contract question — which thread may read a gocui view, what 0 seconds means against a helper that already redefines it, which registries have a resource to overview at all. The mechanical remainder is seven one-line prepends; a plan precise enough for a cheap model would have had to state all of those answers first, which is the whole of the work.

Connected AWS verification: not run, no profile used and no AWS call made. Nothing in this group reads an AWS response shape — no client call was added. A TUI eyeball was not possible either: this cycle has no tty to run the app in, which is the gap `s10-ui-harness` exists to close.

### s4-secrets-overview (7226b1a)

meant: spec.md:11 (an Overview tab consolidating what today is spread across tabs holding a line or two each) and spec.md:12 (two-column, collapses to one column narrow, never soft-wraps, every section degrades to an explicit unavailable state).
did: `ui/presentation/secrets.go:52` `FormatSecretOverview(details, width, now)` — `ResourceHeader` + `secretRotationBadge`, then `Columns` over `secretDetailsBlock` (ARN/created/changed/rotated/next/KMS/description/owning-service, plus a red Status row only when `DeletedDate` is set) and `secretPostureBlock` (replication, resource policy, tags), then `secretVersionsBlock` through `RenderTableFit`.
did: `ui/secrets_panel.go:118` `secretOverview` fetches through the existing `GetSecretDetails` and formats; the registry's placeholder closure is replaced by that method. `ui/overview.go` gains `overviewUnavailableBecause`. `ui/presentation/status.go` gains `insync`/`inprogress` aliases so a replica's state is a badge and not `?`. `ui/presentation/primitives.go:115` gains `kvBlock`, unexported.
did: `ui/presentation/columns.go:52` `truncateBlock` (its own commit, 265d1a0) — the stacked path handed each block back at its own width, so with wrap off an ARN ran off the pane edge unmarked.
deviations: the version id renders WHOLE, where the plan called for a short id — a version id you cannot copy is decoration, and `RenderTableFit` already cuts it when the pane is narrow. The resource policy renders `Configured, shown on the Config tab` rather than the policy JSON: a document in a ~54-cell column is a truncated first line, and the Config tab already prints it in full. Version stages colour by precedence (AWSCURRENT wins over AWSPENDING) because a cell carries one colour and the joint version is the live one; the labels themselves are both shown, so nothing is decided by colour alone. `Columns` belongs to the s1 group and I changed it anyway — this ticket is its first consumer with content wider than a terminal, the fix is one call in one branch, and every stage 5-7 overview inherits it; the s1 sweep was widened to cover the path it used to skip. Badge text is `Rotation every 7d` rather than the plan's `every 7 days`, to match the vocabulary the left panel row already uses. Two blank-line cosmetics left alone: a secret with no `PrimaryRegion` renders `ResourceHeader`'s always-present third line as a blank, and the stacked layout joins the two blocks with a single newline, so `Owning service` sits directly above `Replication` — both are the existing contracts of primitives another group owns.
verified: `make lint` and `make test` from the repo root, both exit 0. Green covers every state the ticket enumerates (joint AWSCURRENT+AWSPENDING, nil stages as `-` and never `[]`, `Not replicated`, `Not configured`, owning service, no tags, no description, pending deletion, the 15-row cap and its `(N more…)` line), the absolute+relative timestamp pairing including a FUTURE next-rotation date, that the value never reaches the pane, and a width sweep 40-200 either side of `minTwoColWidth` asserting no line exceeds its budget. Green does NOT cover the fetch-to-formatter wiring: `Client.Secrets` is a concrete `*secretsmanager.Client` with no interface seam, so no ui-level test can serve a `GetSecretDetails` response, and `TestResourcePanelsOpenOnOverview` only pins the nil-client path. Connected AWS verification NOT run: the read-only staging profile's SSO token has expired ("Token has expired and refresh failed"), and re-authenticating is interactive; no AWS call was made this cycle. Every response shape used here was instead read off the vendored SDK rather than assumed — `SecretVersionsListEntry` (`VersionId`, `VersionStages []string`, `CreatedDate`) and `StatusType` (InSync/Failed/InProgress).
threads: the staging labels are NOT an SDK enum — there is no `SecretVersionStage*` constant in `secretsmanager/types`, so `AWSCURRENT`/`AWSPENDING` are matched as literals; a first draft cited constants that do not exist and did not compile. The missing client seam is the whole of stages 5-7's test gap, not this ticket's alone: every overview will fetch through a concrete SDK client, so if a wiring test is ever wanted, it is one interface on `aws.Client` and not seven. `kvBlock` is unexported and lives with the other overview primitives; the next overview is what proves its shape, and it is where a label-alignment change would go. `secretVersionsShown = 15` is a tune-by-eye number like `minTwoColWidth` and `flexibleFloor`. `s8-refresh-engine` inherits a sharper version of the overlap question `s4-overview-machinery` left: this overview now really does make three AWS calls per tick with no single-flight guard.

### Group notes (s4-secrets-overview)

Not delegated, and it did not look delegable: the ticket is one HUGE member dispatched alone, and the work was a run of judgement calls about what each state should SAY — whole id or short, policy body or policy presence, which stage wins a cell's one colour, whether a stacked block that overruns the pane is this ticket's problem. Writing a plan precise enough for a cheap model would have meant answering all of them first, and the mechanical remainder after that is one formatter and its tests.

### s5-cw-getmetricdata (649fb8b)
meant: spec.md:14 (metrics are billed per metric requested) and spec.md:12 (a section degrades to an explicit state instead of inventing data).
did: `apps/aws/cloudwatch.go:66 instanceMetricQueries` builds all six EC2 queries for one `GetMetricData` (30-min window, `metricPeriod` 300); `:79 latestPoint` searches for the newest datapoint; `:96 mapInstanceMetrics` indexes results by query id; `:112 GetInstanceMetrics` is now one call behind nil-client and empty-id guards. `ui/ec2_panel.go:261 formatMetricPoint` renders `no data` for an absent series and stamps a present one `<value> (<stat> @ 17:43Z)`.
did: `InstanceMetrics` fields changed type from `float64`/`int` to `MetricPoint{Value, At, OK}` — absence is now representable, which is what separates an unpublished disk series from a real zero.
deviations: `GetECSContainerInsights` still runs four serial `GetMetricStatistics` calls in the `ECS/ContainerInsights` namespace — untouched, it is s6-ecs-clusterdata's line. No `NextToken` paging on `GetMetricData`: six ids over a 30-minute window at Period 300 is ~36 datapoints against a 100,800 limit, so the first page is always the whole answer. The stat captions (`5-min avg`, `5-min total`, `5-min max`) are literals at the six call sites rather than carried on the point, because only the formatter needs to know which statistic each field was asked for.
verified: `make lint` and `make test` both exit 0. Covered: the picker (empty/single/unsorted/reversed/zero-value/unpaired slices), id-not-position mapping, absent metrics staying absent, the six queries' names, stats, namespace, period and dimension, both guards, and the rendered `no data` / timestamp line. Corrected by the review (3b3d9db): the period assertion compared the request against the `metricPeriod` constant and so agreed with any value it was set to, the window was asserted nowhere, and the empty-id guard was masked by the nil-client guard — all three are real assertions now, so this line only became true after the review. NOT covered: no test reaches `GetInstanceMetrics`'s own call path — `aws.Client.CloudWatch` is a concrete `*cloudwatch.Client` with no interface seam, so nothing can serve a `GetMetricData` response (the same repo-wide gap s4-secrets-overview recorded). No connected check: the staging profile's SSO token is expired and re-auth is interactive.
threads: the response `Label` field is ignored — CloudWatch returns a per-query label that would let the panel name a metric without a literal, unused for now. `latestPoint` clamps to the shorter of `Timestamps`/`Values`; the API pairs them, nothing in the type system does.

### s5-ec2-datalayer (6fd562c)
meant: spec.md:14 (tiered auto-refresh with single-flight per panel — the cheaper each refresh is, the less the tier costs).
did: `apps/aws/ec2.go:423 volumeIDs` + `:435 applyVolumes` + `:457 DescribeVolumes` replace the per-device `DescribeVolumes` call that ran inside `GetInstanceDetails`'s mapping loop; devices are matched to volumes by `VolumeId`, never by position.
did: `apps/aws/ec2.go:472 mapInstanceTypeInfo` extracted pure from `:522 GetInstanceTypeInfo`, which now answers from `client.go:60 instanceTypes` (a `map[string]InstanceTypeInfo` under `instanceTypesMu`, mirroring the existing `chatModelIDs` cache). The fetch path's map-and-store is `:555 rememberInstanceType` after the review (3b3d9db) — as two adjacent statements the store could be deleted with no test able to notice.
deviations: the cache is consulted BEFORE the nil-EC2 guard — a held answer needs no client, and it is the only way a test can observe the cache without an SDK seam. Cache is per-`Client` and never invalidated (a type's specs cannot change) and never pre-warmed. Volume errors are still swallowed exactly as before, so an unreadable volume costs that device's detail and not the instance view; the error is now dropped at one site instead of per device. Unrequested fix inside the extraction: `ProcessorInfo` was dereferenced unguarded and would panic on a response that omits it — nil-checked now, and `TestMapInstanceTypeInfoToleratesMissingSections` pins it.
verified: `make lint` and `make test` both exit 0. Covered: id collection skipping instance-store devices, the empty list making no call, reversed-order matching, a device whose volume is missing from the answer, both `DescribeVolumes` guards, the full and the bare instance-type mapping, a cache hit answered with no EC2 client, the returned-copy isolation, and both `GetInstanceTypeInfo` guards. NOT covered: `GetInstanceDetails` itself — same missing seam as above, so the wiring from the mapping loop to the batched call was verified by reading the diff.
threads: `DescribeVolumes` is exported because the overview in s5-ec2-overview will want the same batched read; nothing outside `GetInstanceDetails` calls it yet. `ListVolumeSnapshots` still takes volume ids separately and was left alone.

### Group notes (s5-cw-getmetricdata, s5-ec2-datalayer)
Delegation was considered and rejected: two tickets, four files, and every decision in them is a contract call (what absence renders as, whether a cached answer may skip the client guard, whether a swallowed error stays swallowed). Writing a plan precise enough to hand down would have cost more than the edit.
Both tickets are data-layer changes with one UI consumer between them, so the group's real risk is the seam it cannot test. Recorded in each `verified:` line rather than papered over.

### s5-ec2-overview (85f4366)

meant: spec.md:11 (an Overview tab consolidating what today is spread across tabs holding a line or two each) and spec.md:12 (two-column, collapses to one column narrow, never soft-wraps, every section degrades to an explicit unavailable state); the metrics half of spec.md:14 is inherited from `s5-cw-getmetricdata` rather than rebuilt.
did: `apps/aws/instance_overview.go:40 GetInstanceOverview` fans Details/Status/Metrics out behind a WaitGroup and returns an overview rather than an error, per-section failures in `Errs`. `apps/aws/ec2.go:164 describeInstance` is `GetInstanceDetails` minus its `DescribeAddresses` tail, which is what lets the refreshable half be refetched without the Elastic IP lookup riding along.
did: `ui/presentation/ec2.go:51 FormatInstanceOverview` — header from the LIST ROW (so a total fetch failure is still an identified instance), then `Columns` over Configuration/Network/Metrics and Status/Storage/Security/Tags. `:253 instanceStorageBlock` builds its table for `columns.go:54 ColumnWidth`, the new single source of the two-column arithmetic. `:289 encryptionCell`. `:41 MetricReading` is `formatMetricPoint` moved out of the panel; `ui/ec2_panel.go` keeps a one-line alias so its existing tests are what prove the move was behaviour-preserving.
did: `ui/ec2_panel.go:92 instanceOverview` wires the tab; `:120 ec2OverviewExtras.fill` memoizes alarms, ASG and Elastic IPs per (profile generation, instance id). `:304 consoleCapture` dates the Status tab's console log.
deviations: the storage table DROPS the volume id the plan's column list carries — at the narrowest two-column width the column is 52 cells and a 21-cell id crowds out the encryption flag, which is the only actionable thing on the row; the id is on the Storage tab in full. Corrected by the review: that buys the flag room for a normal gp2/gp3 row, NOT for every row — the widest volume EBS can produce still overflows 52 cells and the flag is cut to `encr…`/`unen…`, which is pinned as a readable degradation rather than repaired. Encryption reads `encrypted`/`unencrypted` rather than yes/no because that table has no header row, which also makes the vocabulary differ from the Storage tab's `not encrypted`. ASG membership is rendered as a line INSIDE Status rather than as a section of its own; the ticket names seven sections and it had to live in one of them. `Columns` belongs to the s1 group and I changed it anyway: its stacked path joined the two blocks with a single newline, so below 110 cells the last Metrics row and the `Status` heading read as one section — s4-secrets-overview recorded that wart and inherits the fix, and its two exact-string tests were updated to the new join. Two guards added outside the ticket's words, each preventing a real failure: `GetInstanceDetails`/`GetInstanceStatus` and later `DescribeInstanceAddresses` had no nil-client guard, and the fan-out calls them from goroutines where that is an unrecoverable panic rather than a failed tab. A failed Elastic IP lookup renders `unavailable` where the Config tab still swallows it; stating it is what spec.md:12 asks for and the swallow is pre-existing elsewhere.
verified: `make lint` and `make test` from the repo root, both exit 0, plus `go test -race` over `apps/aws` for the fan-out's shared error map (the gate does not run `-race`, so that is a manual check and not a standing one). Green covers every section rendering, every absence AWS actually reports (no tags/groups/interfaces/EIPs/events/alarms/volumes and a nil ASG), each fetch failing independently and taking exactly the sections that read it, the header surviving a total failure, the throughput column appearing only when a volume reports one, the amber on unencrypted only, the encryption flag surviving at width 110, `ColumnWidth` agreeing with what `Columns` really allots across widths 20-240 and four gaps, a width sweep 40-240 on three overview shapes, the memo being read on a second render and invalidated by either key half, and the console capture stamped, unstamped and absent. Green does NOT cover: the fetch-to-formatter wiring, since `aws.Client`'s SDK clients are concrete structs with no interface seam (the repo-wide gap s4-secrets-overview recorded, unchanged here); how any of it looks on a real terminal.
threads: this overview is the first thing that makes `s8-refresh-engine`'s metric tier urgent rather than tidy. It refetches EVERY refreshable section on the overview interval, so at the default 2s it asks for six billed CloudWatch metrics every two seconds, where spec.md:14 wants ~60s; that ticket owns the fix ("overview tickers refetch metrics only when MetricsSeconds elapsed") and until it lands an open EC2 overview is the most expensive thing in the app. The memo's other edge: alarms and ASG are refetched only when the selection moves, so an alarm that fires while the user sits on one instance does not appear until they select away and back — that is the ticket's contract, not an oversight, but it is the cost of keeping DescribeAlarms off the ticker.
threads: no connected check — the staging profile's SSO token is expired and re-authenticating is interactive, so no AWS call was made this cycle and every response shape used here was read off the module cache instead (`GetConsoleOutputOutput.Timestamp *time.Time`, "the time at which the output was last updated"). The left column runs roughly twice the height of the right one on a healthy instance, so the rule trails down past the last right-hand section; measured, accepted, and a section-balance change would be a layout ticket rather than a formatter one. `ec2OverviewExtras` is one memo on the Gui rather than a per-selection closure, because `overviewTab` hands the render function no per-selection hook; that seam is where a second resource wanting the same treatment would go. The memo holds its lock across two AWS calls, which is also what single-flights them. `launchedAt` parses `InstanceDetails.LaunchTime`, which is a preformatted string from the list mapping rather than a `time.Time`, so an unparseable stamp degrades to itself rather than to a wrong age.

### s6-ecs-clusterdata (70e59f5, b8d57d7)
meant: spec.md:14 (metrics tier priced per metric requested) and the cluster data half of the ECS group; backlog.md:40.
did: `apps/aws/ecs.go:ListECSClusters` sends `Include: [STATISTICS, SETTINGS]`; `mapClusterStatistics` / `containerInsightsSetting` / `ContainerInsightsEnabled` are the pure mappers. `apps/aws/cloudwatch.go:serviceMetricQueries` + `mapServiceMetrics` + `GetECSServiceMetrics` put service CPU/memory on AWS/ECS through GetMetricData; the Insights reservations are four more ids in the SAME request, gated on `Client.clusterInsightsSetting` (recorded by the cluster list, `ecs.go:recordClusterInsights`). `ui/ecs_panel.go:formatECSServiceConfig` renders MetricPoints, so an idle service now reads `0.0% (1-min avg @ …)` where it used to read `n/a`.
deviations: the CLUSTER Config tab still calls `GetECSContainerInsights` on ECS/ContainerInsights, unchanged. Only the service-dimension pair (ClusterName+ServiceName) is verified to return data on AWS/ECS; cluster-level AWS/ECS CPUUtilization is an EC2-launch-type reservation metric that a Fargate-only cluster does not publish, and switching it on an unverified assumption would have replaced an honest `n/a` with a permanently empty section. The ticket's wording is "service CPU/memory metrics", so this is in scope as written, but the cluster tab is now the only ECS surface still reading the Insights namespace as a primary source. `ecsMetricPeriod` is 60 rather than 300: the publish cadence of AWS/ECS service metrics was not verified live, and 60 is safe under either cadence because `latestPoint` reads the newest non-empty bucket and GetMetricData bills per metric, not per datapoint.
verified: `make lint` + `make test` green at each of the two commits. Green covers the mappers, the query shape (namespace/period/stat/dimensions pinned to literals), the Insights gate, and the renderer's zero-vs-absent split. It does NOT cover a live GetMetricData call: no AWS credentials were exercised this cycle, so the AWS/ECS namespace returning data is carried from the ticket's own statement, not re-measured here.
threads: `GetECSContainerInsights` now has exactly one caller (the cluster Config tab). If cluster-level metrics are ever verified on AWS/ECS, that function and `formatUtilizationPercent`, which renders a true 0 as `n/a`, both go. The Insights gate reads a record the cluster list writes, so a service whose cluster has never been listed gets the AWS/ECS pair only; that is the intended conservative default, not a bug, but it means the extras do not appear until the ECS panel has loaded clusters once.

### s6-ecs-image (ff71d1d)
meant: spec.md:13, the hard requirement that ECS views show the image a deployment is actually running; backlog.md:41.
did: `apps/aws/ecs.go` gains `ECSServiceImage` plus `ShortImageRef`, `primaryECSContainer`, `newestECSTask`, `runningECSServiceImage`, `desiredECSServiceImage`, `serviceTaskDefinition` and `ResolveECSServiceImage`. `ECSContainer.Essential` and `ECSTaskDefinitionContainer.Essential` carry the primary-container signal; `ECSDeployment.TaskDefinition` carries the PRIMARY deployment's revision. `DescribeTaskDefinitionDetail` memoizes pinned revisions on `Client.taskDefs`. `ui/presentation/ecs.go:ECSImageSummary`/`ECSImageLabel` render it; the service Config tab shows it.
deviations: the image is rendered on the service Config tab, not on the service ROW: a per-row image needs one DescribeTasks per service, which is the N+1 the list load must not become. The cluster and service Overview tabs are still `overviewUnavailable` placeholders (they belong to s6-ecs-cluster-overview / s6-ecs-service-overview), so this group's resolver has one call site today. `newestECSTask` picks ONE task mid-rollout rather than reporting that two revisions are live; that is a real loss of information, chosen because a single deterministic answer beats whichever task the API happened to list first. The cached task-definition detail is shared by pointer rather than deep-copied, so callers must read it without editing it; the EC2 instance-type cache stores values instead, and this one differs because the detail holds nested maps.
verified: `make lint` + `make test` green. Tests cover multi-container, the sidecar-only difference (two tasks differing only in their sidecar resolve to the same primary image), zero running tasks routing to the desired-image fallback, stopped tasks being skipped, response order not deciding, the creation-time fallback for a task that never recorded a start, and the pinned-vs-floating cache rule. NOT covered: a live DescribeTasks; the image field's shape is carried from the ticket, not re-measured.
threads: `primaryECSContainer` falls back to the first container when nothing is marked essential, which is also what a task whose definition failed to load looks like, so the two are indistinguishable at the call site. `ResolveECSServiceImage` calls `ListECSTasks`, which itself describes the task definition for every task it builds; on the 2s overview tier that is a call worth re-costing before the overview tickets consume it.
